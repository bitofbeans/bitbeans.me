# bitbeans.me
My personal website, hosted on Netlify. 

I use Vite to build; commands are `npm run dev` and `npm run build`.

Note: Netlify auto-builds the website themselves, no need to use build command to publish new changes.
